---
phase: 01-data-pipeline
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/collectors/__tests__/inrix-speeds.test.ts
  - backend/src/collectors/__tests__/inrix-incidents.test.ts
  - backend/src/collectors/__tests__/weather.test.ts
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07

must_haves:
  truths:
    - "TypeScript project compiles with strict mode (npx tsc --noEmit exits 0)"
    - "All 51 existing tests still pass after the type fix"
  artifacts:
    - path: "backend/src/collectors/__tests__/inrix-speeds.test.ts"
      provides: "Type-safe axios mock in speed collector tests"
      contains: "vi.mocked(axios.get)"
    - path: "backend/src/collectors/__tests__/inrix-incidents.test.ts"
      provides: "Type-safe axios mock in incident collector tests"
      contains: "vi.mocked(axios.get)"
    - path: "backend/src/collectors/__tests__/weather.test.ts"
      provides: "Type-safe axios mock in weather collector tests"
      contains: "vi.mocked(axios.get)"
  key_links:
    - from: "backend/src/collectors/__tests__/inrix-speeds.test.ts"
      to: "axios"
      via: "vi.mocked(axios.get) typed mock"
      pattern: "vi\\.mocked\\(axios\\.get\\)"
    - from: "backend/src/collectors/__tests__/inrix-incidents.test.ts"
      to: "axios"
      via: "vi.mocked(axios.get) typed mock"
      pattern: "vi\\.mocked\\(axios\\.get\\)"
    - from: "backend/src/collectors/__tests__/weather.test.ts"
      to: "axios"
      via: "vi.mocked(axios.get) typed mock"
      pattern: "vi\\.mocked\\(axios\\.get\\)"
---

<objective>
Fix TypeScript strict-mode compilation errors in collector test files.

Purpose: Close the single verification gap from Phase 1 -- `npx tsc --noEmit` fails with 17 type errors because `vi.mocked(axios).get` does not carry vitest mock method types. The fix is to mock `axios.get` directly so TypeScript recognizes `.mockResolvedValue`, `.mockImplementation`, and `.mockRejectedValue`.

Output: Three updated test files that compile cleanly under `tsc --noEmit --strict`.
</objective>

<execution_context>
@C:/Users/FisherDoan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FisherDoan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-pipeline/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix vi.mocked(axios) typing in all three collector test files</name>
  <files>
    backend/src/collectors/__tests__/inrix-speeds.test.ts
    backend/src/collectors/__tests__/inrix-incidents.test.ts
    backend/src/collectors/__tests__/weather.test.ts
  </files>
  <read_first>
    backend/src/collectors/__tests__/inrix-speeds.test.ts
    backend/src/collectors/__tests__/inrix-incidents.test.ts
    backend/src/collectors/__tests__/weather.test.ts
    backend/tsconfig.json
  </read_first>
  <action>
In each of the three collector test files, apply the same transformation:

1. Replace the line:
   `const mockedAxios = vi.mocked(axios);`
   with:
   `const mockedAxiosGet = vi.mocked(axios.get);`

2. Replace every occurrence of `mockedAxios.get.mockResolvedValue` with `mockedAxiosGet.mockResolvedValue`.

3. Replace every occurrence of `mockedAxios.get.mockImplementation` with `mockedAxiosGet.mockImplementation`.

4. Replace every occurrence of `mockedAxios.get.mockRejectedValue` with `mockedAxiosGet.mockRejectedValue`.

5. Replace every occurrence of `expect(mockedAxios.get)` with `expect(mockedAxiosGet)`.

The specific lines affected per file:

**inrix-speeds.test.ts** (line 27, then lines 86, 90, 112, 117, 139, 161, 169, 179):
- Line 27: `const mockedAxios = vi.mocked(axios);` -> `const mockedAxiosGet = vi.mocked(axios.get);`
- Lines 86, 117, 169, 179: `mockedAxios.get.mockResolvedValue(...)` -> `mockedAxiosGet.mockResolvedValue(...)`
- Lines 90, 112: `expect(mockedAxios.get)` -> `expect(mockedAxiosGet)`
- Line 139: `mockedAxios.get.mockImplementation(...)` -> `mockedAxiosGet.mockImplementation(...)`
- Line 161: `mockedAxios.get.mockRejectedValue(...)` -> `mockedAxiosGet.mockRejectedValue(...)`

**inrix-incidents.test.ts** (line 27, then lines 86, 90, 108, 113, 135, 156, 164):
- Line 27: `const mockedAxios = vi.mocked(axios);` -> `const mockedAxiosGet = vi.mocked(axios.get);`
- Lines 86, 113, 164: `mockedAxios.get.mockResolvedValue(...)` -> `mockedAxiosGet.mockResolvedValue(...)`
- Lines 90, 108: `expect(mockedAxios.get)` -> `expect(mockedAxiosGet)`
- Line 135: `mockedAxios.get.mockImplementation(...)` -> `mockedAxiosGet.mockImplementation(...)`
- Line 156: `mockedAxios.get.mockRejectedValue(...)` -> `mockedAxiosGet.mockRejectedValue(...)`

**weather.test.ts** (line 25, then lines 53, 57, 74, 80, 109, 119, 130):
- Line 25: `const mockedAxios = vi.mocked(axios);` -> `const mockedAxiosGet = vi.mocked(axios.get);`
- Lines 53, 80, 109, 130: `mockedAxios.get.mockResolvedValue(...)` -> `mockedAxiosGet.mockResolvedValue(...)`
- Lines 57: `expect(mockedAxios.get)` -> `expect(mockedAxiosGet)`
- Line 74: `mockedAxios.get.mockResolvedValue(...)` -> `mockedAxiosGet.mockResolvedValue(...)`
- Line 119: (same pattern as above)

Do NOT change any test logic, assertions, mock data, or other mock declarations. Only change the axios mock variable declaration and its usage sites.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0 (zero errors)
    - All 51 tests pass (same count as before the change)
    - No test file contains the string `vi.mocked(axios);` (the old untyped pattern)
    - Every test file contains `vi.mocked(axios.get)` instead
  </acceptance_criteria>
  <done>TypeScript compiles cleanly under strict mode. All 51 unit tests pass. The verification gap "TypeScript project compiles with strict mode" is closed.</done>
</task>

</tasks>

<verification>
1. `cd backend && npx tsc --noEmit` exits 0 (was exit code 2 with 17 errors)
2. `cd backend && npx vitest run` shows 51/51 tests passing
3. `grep -r "vi.mocked(axios);" backend/src/collectors/__tests__/` returns no matches (old pattern gone)
4. `grep -r "vi.mocked(axios.get)" backend/src/collectors/__tests__/` returns 3 matches (one per file)
</verification>

<success_criteria>
- npx tsc --noEmit exits 0 with zero type errors
- 51/51 tests pass with no regressions
- VERIFICATION.md gap on "TypeScript project compiles with strict mode" is resolved
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline/01-04-SUMMARY.md`
</output>
