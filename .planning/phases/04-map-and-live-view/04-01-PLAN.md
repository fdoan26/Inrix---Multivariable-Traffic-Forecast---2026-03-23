---
phase: 04-map-and-live-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/vitest.config.ts
  - frontend/eslint.config.js
  - frontend/index.html
  - frontend/.env.development
  - frontend/.gitignore
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/vite-env.d.ts
  - frontend/src/test/setup.ts
  - frontend/src/types/api.ts
  - frontend/src/data/corridors.ts
  - frontend/src/lib/api.ts
  - frontend/src/store/mapStore.ts
  - frontend/src/hooks/useCorridorSpeeds.ts
  - frontend/src/components/MapView.tsx
  - frontend/src/components/CorridorPanel.tsx
  - frontend/src/data/__tests__/corridors.test.ts
  - frontend/src/components/__tests__/CorridorPanel.test.tsx
autonomous: true
requirements:
  - MAP-01
  - MAP-02

must_haves:
  truths:
    - "Interactive Mapbox map loads centered on SF at [-122.4194, 37.7749] zoom 12 with dark-v11 style"
    - "6 corridor lines render on the map as GeoJSON LineStrings with data-driven congestion colors"
    - "Corridor speeds auto-refresh every 5 minutes via TanStack Query"
    - "Floating panel shows corridor list with congestion badges and last-updated timestamp"
    - "Clicking a corridor highlights it and shows speed details in the panel"
    - "Panel is responsive: left sidebar on desktop, bottom sheet on mobile"
  artifacts:
    - path: "frontend/src/components/MapView.tsx"
      provides: "Map rendering with corridor Source/Layer"
      contains: "react-map-gl/mapbox"
    - path: "frontend/src/components/CorridorPanel.tsx"
      provides: "Floating panel with corridor list"
      contains: "congestion"
    - path: "frontend/src/data/corridors.ts"
      provides: "GeoJSON FeatureCollection for 6 corridors"
      contains: "us-101"
    - path: "frontend/src/hooks/useCorridorSpeeds.ts"
      provides: "TanStack Query hook with 5-min refetch"
      contains: "refetchInterval"
    - path: "frontend/src/store/mapStore.ts"
      provides: "Zustand UI state store"
      contains: "selectedCorridorId"
    - path: "frontend/src/types/api.ts"
      provides: "TypeScript interfaces for API responses"
      exports: ["CorridorCurrentResponse", "CongestionLevel"]
  key_links:
    - from: "frontend/src/hooks/useCorridorSpeeds.ts"
      to: "/api/corridors/:id/current"
      via: "fetch with VITE_API_URL"
      pattern: "api/corridors"
    - from: "frontend/src/components/MapView.tsx"
      to: "frontend/src/data/corridors.ts"
      via: "GeoJSON data prop on Source"
      pattern: "createCorridorFeatureCollection"
    - from: "frontend/src/components/MapView.tsx"
      to: "frontend/src/store/mapStore.ts"
      via: "Zustand selector for selectedCorridorId"
      pattern: "useMapStore"
---

<objective>
Scaffold the React/Vite/TypeScript frontend app and deliver the interactive Mapbox map with live corridor speed visualization and a responsive floating panel.

Purpose: This is the first user-visible output of the entire project. Users see a dark-themed map of SF with 6 major corridors color-coded by congestion level (green/amber/red), auto-refreshing every 5 minutes.

Output: Working frontend app at `http://localhost:5173` with Mapbox map, corridor lines, speed data from backend API, and responsive panel.
</objective>

<execution_context>
@C:/Users/FisherDoan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FisherDoan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-map-and-live-view/04-CONTEXT.md
@.planning/phases/04-map-and-live-view/04-RESEARCH.md

<interfaces>
<!-- Backend API contract consumed by frontend hooks -->

From backend/src/api/corridors.ts — GET /api/corridors/:corridorId/current:
```typescript
// Response shape:
{
  corridor_id: string;        // 'us-101', 'i-280', 'bay-bridge', 'van-ness', '19th-ave', 'market-st'
  display_name: string;       // 'US-101', 'I-280', 'Bay Bridge Approach', etc.
  congestion_level: string;   // 'free_flow' | 'moderate' | 'heavy'
  avg_travel_time_min: number;
  segments: Array<{
    segment_id: string;
    speed: number;
    congestion_score: number;
    travel_time_min: number;
    recorded_at: string;
  }>;
}
```

From backend/src/db/migrations/004_create-corridors-table.sql — canonical corridor IDs:
```sql
-- THESE are the correct IDs (NOT directional us-101-n, us-101-s):
'us-101'      -- 'US-101'
'i-280'       -- 'I-280'
'bay-bridge'  -- 'Bay Bridge Approach'
'van-ness'    -- 'Van Ness Ave'
'19th-ave'    -- '19th Ave'
'market-st'   -- 'Market St'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold frontend app with Vite, Tailwind v4, Mapbox, TanStack Query, Zustand, and test infrastructure</name>
  <files>
    frontend/package.json,
    frontend/vite.config.ts,
    frontend/tsconfig.json,
    frontend/tsconfig.app.json,
    frontend/tsconfig.node.json,
    frontend/vitest.config.ts,
    frontend/eslint.config.js,
    frontend/index.html,
    frontend/.env.development,
    frontend/.gitignore,
    frontend/src/main.tsx,
    frontend/src/App.tsx,
    frontend/src/index.css,
    frontend/src/vite-env.d.ts,
    frontend/src/test/setup.ts,
    frontend/src/types/api.ts,
    frontend/src/data/corridors.ts,
    frontend/src/lib/api.ts,
    frontend/src/store/mapStore.ts,
    frontend/src/data/__tests__/corridors.test.ts
  </files>
  <read_first>
    backend/src/api/corridors.ts,
    backend/src/db/migrations/004_create-corridors-table.sql,
    .planning/phases/04-map-and-live-view/04-RESEARCH.md
  </read_first>
  <action>
    1. Run `npm create vite@latest frontend -- --template react-ts` from repo root. Then `cd frontend`.

    2. Install dependencies:
       ```
       npm install mapbox-gl react-map-gl @tanstack/react-query zustand tailwindcss @tailwindcss/vite date-fns
       npm install -D @types/mapbox-gl @testing-library/react @testing-library/jest-dom jsdom vitest
       ```

    3. Configure `vite.config.ts`:
       - Import `react` from `@vitejs/plugin-react`, `tailwindcss` from `@tailwindcss/vite`
       - Plugins: `[react(), tailwindcss()]`
       - `resolve.alias`: `{ '@': path.resolve(__dirname, './src') }`
       - Do NOT create a `tailwind.config.js` -- Tailwind v4 uses `@tailwindcss/vite` plugin only

    4. Configure `tsconfig.json` (project references style):
       - References: `["./tsconfig.app.json", "./tsconfig.node.json"]`
       - `tsconfig.app.json`: `compilerOptions.paths`: `{ "@/*": ["./src/*"] }`
       - Ensure `moduleResolution: "bundler"` and `jsx: "react-jsx"`

    5. Configure `vitest.config.ts`:
       ```typescript
       import { defineConfig } from 'vitest/config';
       import path from 'path';
       export default defineConfig({
         test: {
           environment: 'jsdom',
           setupFiles: ['./src/test/setup.ts'],
           include: ['src/**/*.test.{ts,tsx}'],
         },
         resolve: {
           alias: { '@': path.resolve(__dirname, './src') },
         },
       });
       ```

    6. Create `src/test/setup.ts`:
       ```typescript
       import '@testing-library/jest-dom';
       ```

    7. Create `src/index.css` with ONLY `@import "tailwindcss";` -- no tailwind.config.js, no PostCSS config.

    8. Create `src/main.tsx`:
       - Import `mapbox-gl/dist/mapbox-gl.css` (CRITICAL: must be imported for map to render correctly)
       - Import `./index.css`
       - Wrap app in `QueryClientProvider` from `@tanstack/react-query`
       - QueryClient config: `defaultOptions.queries.staleTime: 4 * 60 * 1000`
       - Render `<App />`

    9. Create `frontend/.env.development`:
       ```
       VITE_MAPBOX_TOKEN=your_mapbox_token_here
       VITE_API_URL=http://localhost:3001
       ```
       Add `.env*.local` and `.env.development` to `.gitignore`.

    10. Create `src/types/api.ts` with TypeScript interfaces:
        ```typescript
        export type CongestionLevel = 'free_flow' | 'moderate' | 'heavy' | 'unknown';

        export interface CorridorSegment {
          segment_id: string;
          speed: number;
          congestion_score: number;
          travel_time_min: number;
          recorded_at: string;
        }

        export interface CorridorCurrentResponse {
          corridor_id: string;
          display_name: string;
          congestion_level: CongestionLevel;
          avg_travel_time_min: number;
          segments: CorridorSegment[];
        }
        ```

    11. Create `src/lib/api.ts`:
        ```typescript
        const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3001';

        export async function fetchCorridorSpeed(corridorId: string): Promise<CorridorCurrentResponse> {
          const res = await fetch(`${API_URL}/api/corridors/${corridorId}/current`);
          if (!res.ok) throw new Error(`Failed to fetch corridor ${corridorId}: ${res.status}`);
          return res.json();
        }
        ```

    12. Create `src/store/mapStore.ts` using Zustand v5 `create`:
        - State: `selectedCorridorId: string | null`, `incidentsVisible: boolean` (default true), `lastUpdated: Date | null`
        - Actions: `selectCorridor(id: string | null)`, `toggleIncidents()`, `setLastUpdated(date: Date)`

    13. Create `src/data/corridors.ts`:
        - Export `CORRIDOR_IDS = ['us-101', 'i-280', 'bay-bridge', 'van-ness', '19th-ave', 'market-st'] as const`
          CRITICAL: Use database canonical IDs (NOT directional us-101-n, us-101-s from CONTEXT.md)
        - Export `CONGESTION_COLORS = { free_flow: '#10b981', moderate: '#f59e0b', heavy: '#ef4444', unknown: '#6b7280' } as const`
        - Export `CORRIDOR_GEOMETRIES`: Record mapping corridor ID to GeoJSON LineString coordinates approximating real SF roads:
          - `us-101`: Route along 101 through SF from ~Candlestick Point to Golden Gate approach, approx coords: [[-122.3870, 37.7095], [-122.3990, 37.7280], [-122.4050, 37.7470], [-122.4150, 37.7620], [-122.4350, 37.7840], [-122.4490, 37.7960]]
          - `i-280`: Route along 280 from Daly City to downtown SF, approx: [[-122.4480, 37.6890], [-122.4380, 37.7110], [-122.4150, 37.7350], [-122.3950, 37.7570]]
          - `bay-bridge`: I-80 approach from Treasure Island to SF, approx: [[-122.3700, 37.8100], [-122.3780, 37.8020], [-122.3880, 37.7900], [-122.3930, 37.7830]]
          - `van-ness`: Van Ness Ave from Market to Bay, approx: [[-122.4194, 37.7749], [-122.4218, 37.7900], [-122.4235, 37.8050]]
          - `19th-ave`: 19th Ave from Golden Gate Park south, approx: [[-122.4755, 37.7550], [-122.4755, 37.7400], [-122.4755, 37.7250], [-122.4755, 37.7100]]
          - `market-st`: Market St from Embarcadero to Castro, approx: [[-122.3934, 37.7946], [-122.4070, 37.7870], [-122.4194, 37.7749], [-122.4350, 37.7630]]
        - Export `CORRIDOR_DISPLAY_NAMES`: Record mapping corridor ID to display name
        - Export `createCorridorFeatureCollection(corridorData: Array<{ id: string; congestionLevel: CongestionLevel }>): FeatureCollection` function that creates a GeoJSON FeatureCollection with one Feature per corridor, properties including `corridorId` and `color` (from CONGESTION_COLORS lookup).

    14. Create `src/data/__tests__/corridors.test.ts`:
        - Test: `CORRIDOR_IDS` has exactly 6 entries
        - Test: Each corridor ID has a corresponding geometry in `CORRIDOR_GEOMETRIES`
        - Test: Each geometry is a valid array of [lng, lat] coordinate pairs
        - Test: `createCorridorFeatureCollection` returns FeatureCollection with 6 features
        - Test: Feature properties include `corridorId` and `color`
        - Test: `CONGESTION_COLORS` has keys free_flow, moderate, heavy, unknown

    15. Create `src/hooks/useCorridorSpeeds.ts`:
        - Use `useQueries` from `@tanstack/react-query`
        - Query each of the 6 `CORRIDOR_IDS` with `queryKey: ['corridor-speed', id]`
        - `queryFn`: call `fetchCorridorSpeed(id)` from `@/lib/api`
        - `refetchInterval: 5 * 60 * 1000` (5 minutes)
        - `staleTime: 4 * 60 * 1000` (4 minutes)
        - Return combined results with a helper that maps responses to corridor data array

    16. Create minimal `src/App.tsx` that renders `<MapView />` and `<CorridorPanel />`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/data/__tests__/corridors.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` exists with react, mapbox-gl, react-map-gl, @tanstack/react-query, zustand, tailwindcss, @tailwindcss/vite as dependencies
    - `frontend/vite.config.ts` imports `@tailwindcss/vite` and has `@` path alias
    - `frontend/src/index.css` contains `@import "tailwindcss"` and NO tailwind.config.js exists
    - `frontend/src/main.tsx` imports `mapbox-gl/dist/mapbox-gl.css` and wraps app in QueryClientProvider
    - `frontend/src/types/api.ts` exports `CongestionLevel` and `CorridorCurrentResponse`
    - `frontend/src/data/corridors.ts` uses IDs `us-101`, `i-280`, `bay-bridge`, `van-ness`, `19th-ave`, `market-st` (NOT directional)
    - `frontend/src/store/mapStore.ts` exports `useMapStore` with `selectedCorridorId` and `incidentsVisible`
    - `frontend/src/hooks/useCorridorSpeeds.ts` uses `useQueries` with `refetchInterval: 5 * 60 * 1000`
    - `frontend/src/lib/api.ts` uses `import.meta.env.VITE_API_URL`
    - `npx vitest run src/data/__tests__/corridors.test.ts` passes
  </acceptance_criteria>
  <done>Frontend scaffold fully initialized with all dependencies, types, data layer, store, hooks, and test infrastructure. Corridor GeoJSON test suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Build MapView component with corridor rendering and CorridorPanel with responsive layout</name>
  <files>
    frontend/src/components/MapView.tsx,
    frontend/src/components/CorridorPanel.tsx,
    frontend/src/App.tsx,
    frontend/src/components/__tests__/CorridorPanel.test.tsx
  </files>
  <read_first>
    frontend/src/types/api.ts,
    frontend/src/data/corridors.ts,
    frontend/src/hooks/useCorridorSpeeds.ts,
    frontend/src/store/mapStore.ts,
    .planning/phases/04-map-and-live-view/04-RESEARCH.md
  </read_first>
  <action>
    1. Create `src/components/MapView.tsx`:
       - Import `Map, Source, Layer` from `react-map-gl/mapbox` (CRITICAL: NOT bare `react-map-gl`)
       - Import `mapbox-gl` types: `import type { LineLayer, MapMouseEvent } from 'mapbox-gl'`
       - Props: none (self-contained, uses hooks internally)
       - Use `useCorridorSpeeds()` hook to get speed data
       - Use `useMapStore` to get/set `selectedCorridorId`
       - Build GeoJSON using `createCorridorFeatureCollection()` from `@/data/corridors`
         - Map each corridor speed response's `congestion_level` to the CongestionLevel type
         - For corridors without data yet, use `'unknown'` as congestion level
         - CRITICAL: Memoize with `useMemo`, recompute only when speed query data changes
       - Render `<Map>` component:
         - `mapboxAccessToken={import.meta.env.VITE_MAPBOX_TOKEN}`
         - `initialViewState={{ longitude: -122.4194, latitude: 37.7749, zoom: 12 }}`
         - `style={{ width: '100vw', height: '100vh' }}`
         - `mapStyle="mapbox://styles/mapbox/dark-v11"`
         - `interactiveLayerIds={['corridor-lines']}`
         - `onClick` handler: extract `corridorId` from clicked feature properties, call `selectCorridor(id)` from store. If click is on empty space, deselect (`selectCorridor(null)`).
       - Inside Map, render `<Source id="corridors" type="geojson" data={corridorGeoJSON}>`:
         - `<Layer>` with `id="corridor-lines"`, `type="line"`:
           - `paint['line-color']`: `['get', 'color']`
           - `paint['line-width']`: `6`
           - `paint['line-opacity']`: Use `case` expression -- if `selectedCorridorId` is set, selected corridor gets `1.0`, others get `0.6`. If no selection, all get `0.8`.
           - `layout['line-cap']`: `'round'`
           - `layout['line-join']`: `'round'`

    2. Create `src/components/CorridorPanel.tsx`:
       - Uses `useCorridorSpeeds()` hook and `useMapStore` store
       - Layout: floating panel with `position: fixed`
         - Desktop (>= 768px): `left-0 top-0 h-full w-80` (320px wide, full height)
         - Mobile (< 768px): `bottom-0 left-0 w-full h-64` (slides up from bottom)
         - Use Tailwind responsive: base styles for mobile, `md:` prefix for desktop
       - Styling: `bg-gray-900/95 backdrop-blur-sm text-gray-100 border-r border-gray-700` (dark theme matching dark-v11)
       - Content:
         a. Header: "SF Traffic" title + last-updated timestamp using `formatDistanceToNow` from date-fns ("Updated 2 min ago")
         b. Loading indicator: show a subtle spinner/text when any query is fetching
         c. Corridor list: For each corridor, render a row with:
            - Display name (from API response or `CORRIDOR_DISPLAY_NAMES` fallback)
            - Congestion badge: colored dot using `CONGESTION_COLORS[congestion_level]` with label text
            - `avg_travel_time_min` displayed as "X.X min"
            - Click handler: `selectCorridor(corridor.corridor_id)`
            - Selected state: highlight row with `bg-gray-700/50` or similar
         d. Selected corridor detail: When a corridor is selected, show expanded segment details below the list
         e. Incident toggle checkbox (just the UI control, wired to `toggleIncidents()` in store -- actual incidents come in Plan 02)
       - Panel has `overflow-y-auto` for scrolling on mobile

    3. Update `src/App.tsx`:
       - Import and render `<MapView />` and `<CorridorPanel />`
       - No wrapping div needed -- MapView is full-viewport, Panel is position-fixed overlay

    4. Create `src/components/__tests__/CorridorPanel.test.tsx`:
       - Mock `useCorridorSpeeds` to return fake corridor data with different congestion levels
       - Mock `useMapStore` to provide selectCorridor and selectedCorridorId
       - Test: renders all 6 corridor names
       - Test: shows congestion badge for each corridor
       - Test: clicking a corridor calls selectCorridor with correct ID
       - Test: shows "Updated X ago" timestamp text when lastUpdated is set
       - Test: renders incident toggle checkbox
       - Use `@testing-library/react` render, screen, fireEvent
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/MapView.tsx` imports from `react-map-gl/mapbox` (not bare `react-map-gl`)
    - MapView uses `<Source type="geojson">` with `<Layer type="line">` (not imperative addSource/addLayer)
    - MapView uses `interactiveLayerIds={['corridor-lines']}` for click detection
    - MapView memoizes GeoJSON data with `useMemo`
    - CorridorPanel has responsive classes: `md:w-80 md:h-full` for desktop, mobile bottom-sheet
    - CorridorPanel uses `bg-gray-900` dark theme styling
    - CorridorPanel shows congestion badges with colors from `CONGESTION_COLORS`
    - CorridorPanel has incident layer toggle checkbox
    - All vitest tests pass including CorridorPanel.test.tsx
  </acceptance_criteria>
  <done>MapView renders Mapbox map with dark-v11 style and 6 corridor lines colored by congestion level. CorridorPanel shows corridor list with congestion badges, responds to clicks, and is responsive across desktop/mobile. All component tests green.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vitest run --reporter=verbose` -- all unit tests green
2. `cd frontend && npx tsc --noEmit` -- no TypeScript errors
3. `cd frontend && npx vite build` -- production build succeeds
4. Manual: `cd frontend && npm run dev` -- map loads at localhost:5173 with dark style and corridor lines (requires MAPBOX_TOKEN)
</verification>

<success_criteria>
- Frontend app scaffolded in `frontend/` with React 19, Vite, TypeScript, Tailwind v4, Mapbox GL JS, react-map-gl, TanStack Query, Zustand
- 6 corridor lines render on map with data-driven congestion colors
- Corridor speeds auto-refresh every 5 minutes from backend API
- Floating panel shows corridor list with congestion badges and last-updated timestamp
- Clicking a corridor highlights it on the map and in the panel
- Panel is responsive: left sidebar on desktop, bottom sheet on mobile
- All tests pass, TypeScript compiles, production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-map-and-live-view/04-01-SUMMARY.md`
</output>
